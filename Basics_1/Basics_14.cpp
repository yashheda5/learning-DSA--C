#include <iostream>
using namespace std;
int main(){
    int n=1;
    //finding the address of n in the memory 
    //using the addressof operator 
    cout<<"the address of a is = "<<endl;
    cout<< &n <<endl;
}